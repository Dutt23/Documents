// Create new text file. 

Open automator. Library , select run AppleScript. 
Paste tell application "Finder" to make new file at (the target of the front window) as alias run it. 
Save as NewTextApp.app. Use control + cmd to drag that file into the toolbar of the finder and use. 

// Enable terminal in folders.
Apple icon on the left , system preferences. Services. From there tick which ever one you need to for the enabling. 

// Viewing hidden files on Mac 

Cmd + shift + .
Ss
// Brew , package installer
 Link : /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"

Make sure you install in the root directory. (Maybe not the correct word , but you know what I mean)

Brew install wget
Ss
//
To check location do which {{Thing name }}

//Ports
sudo lsof -PiTCP -sTCP:LISTEN

//
Configure .bash_profile for mvn 
export M2_HOME=/usr/local/Cellar/maven/3.5.0/libexec
    export M2=${M2_HOME}/bin
    export PATH=${PATH}:${M2_HOME}/bin

// Open up terminal  

Option + Cmd + esc
// Lombok
Mac : open finder. Open the eclipse.ini file inside Contents/Eclipse and tase -javaagent:/Applications/Eclipse.app/Contents/MacOS/lombok-1.16.18.jar on the last line. 
<<<<<<< HEAD

//MYSQL
Brew install mysql
Brew tap services/ check online
Then type mysql_secure_installation
Make sure your mysql version in the pom and your system mysql version is the same 
CREATE USER 'newuser'@'localhost' IDENTIFIED BY 'password';
Grant all on table_name.* to 'root'
=======
>>>>>>> f53f35ee6d72c39a74a4be65a903a2fb31960602

Command + D to select the thing.
Then options plus arrow key to move the line of code
